%p#notice= notice
%h1 Pokemons
.float-right
  = link_to trainers_path do
    .btn.btn-primary
      = 'Entrenadores'
%table.table.table-striped
  %thead
    %tr
      %th{:colspan => "3"}



  %tbody
    - @pokemon_list.each do |pokemon_list|
      %tr
        %td= pokemon_list['name']


        %td.d-flex.justify-content-end
          = link_to pokemon_capturado_pokemon_path(url: pokemon_list['url'] ) do
            .btn.btn-primary
              = 'capturar'
